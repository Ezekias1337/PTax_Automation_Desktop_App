const { until } = require("selenium-webdriver");
const swapToIFrameDefaultContent = require("../../frameSwaps/swapToIFrameDefaultContent");
const { navbarFileSelectors } = require("../../../../ptaxXpathsAndSelectors/allSelectors");

const fileNavbarClick = async (driver, subOption) => {
    
};

module.exports = fileNavbarClick;
