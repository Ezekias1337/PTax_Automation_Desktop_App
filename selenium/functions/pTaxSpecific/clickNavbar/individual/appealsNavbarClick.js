const { until } = require("selenium-webdriver");
const swapToIFrameDefaultContent = require("../../frameSwaps/swapToIFrameDefaultContent");
const { navbarAppealsSelectors } = require("../../../../ptaxXpathsAndSelectors/allSelectors");

const appealsNavbarClick = async (driver, subOption) => {
    
};

module.exports = appealsNavbarClick;
