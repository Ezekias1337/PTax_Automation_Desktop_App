const { until } = require("selenium-webdriver");
const swapToIFrameDefaultContent = require("../../frameSwaps/swapToIFrameDefaultContent");
const { navbarPaymentsSelectors } = require("../../../../ptaxXpathsAndSelectors/allSelectors");

const paymentsNavbarClick = async (driver, subOption) => {
    
};

module.exports = paymentsNavbarClick;
