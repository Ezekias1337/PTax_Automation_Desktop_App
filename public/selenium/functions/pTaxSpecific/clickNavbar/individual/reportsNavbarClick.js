const { until } = require("selenium-webdriver");
const swapToIFrameDefaultContent = require("../../frameSwaps/swapToIFrameDefaultContent");
const { navbarReportsSelectors } = require("../../../../ptaxXpathsAndSelectors/allSelectors");

const reportsNavbarClick = async (driver, subOption) => {
    
};

module.exports = reportsNavbarClick;
