const { until } = require("selenium-webdriver");
const swapToIFrameDefaultContent = require("../../frameSwaps/swapToIFrameDefaultContent");
const { navbarViewSelectors } = require("../../../../ptaxXpathsAndSelectors/allSelectors");

const viewNavbarClick = async (driver, subOption) => {
    
};

module.exports = viewNavbarClick;
