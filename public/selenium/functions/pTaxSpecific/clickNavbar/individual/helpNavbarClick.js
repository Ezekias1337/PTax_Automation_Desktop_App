const { until } = require("selenium-webdriver");
const swapToIFrameDefaultContent = require("../../frameSwaps/swapToIFrameDefaultContent");
const { navbarHelpSelectors } = require("../../../../ptaxXpathsAndSelectors/allSelectors");

const helpNavbarClick = async (driver, subOption) => {
    
};

module.exports = helpNavbarClick;
