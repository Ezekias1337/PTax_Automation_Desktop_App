const { until } = require("selenium-webdriver");
const swapToIFrameDefaultContent = require("../../frameSwaps/swapToIFrameDefaultContent");
const { navbarLinksSelectors } = require("../../../../ptaxXpathsAndSelectors/allSelectors");

const linksNavbarClick = async (driver, subOption) => {
    
};

module.exports = linksNavbarClick;
